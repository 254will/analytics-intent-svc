# analytics-intent-svc
Intent Analytics API — a FastAPI microservice that reads structured data and returns analytics-grade insights using a fine-tuned model. CPU-optimized, container-ready, and deployable.
